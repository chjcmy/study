# Matrix Factorization (MF)

## 📌 개념 (Concept)
추천 시스템의 가장 기본이 되는 모델로, **"사용자(User)와 아이템(Item)의 상호작용 행렬을 두 개의 저차원 행렬로 분해"**하는 방식입니다.

- **핵심 원리**: `Dot Product` (내적)
- **비유**: 
    - 유저의 취향을 나타내는 벡터 (예: [액션: 0.9, 로맨스: 0.1])
    - 아이템의 특성을 나타내는 벡터 (예: [액션: 0.8, 로맨스: 0.2])
    - 두 벡터를 곱해서 평점을 예측 (0.9*0.8 + 0.1*0.2 = 0.74)

## ⚙️ 구조 (Architecture)
1.  **Embedding Layer**: 유저와 아이템을 고정된 크기(예: 32차원)의 벡터로 변환
2.  **Dot Product**: 유저 벡터와 아이템 벡터를 곱함
3.  **Output**: 예측 평점 (Score)

## 👍 장점 (Pros)
- **단순함**: 구조가 간단해서 이해하기 쉽고 구현이 빠름.
- **속도**: 학습과 추론 속도가 매우 빠름.
- **가성비**: 적은 리소스로 준수한 성능을 냄. (Baseline으로 적합)

## 👎 단점 (Cons)
- **단순한 관계**: 선형적인 관계(Linear)만 파악 가능. 복잡한 비선형적 패턴은 놓칠 수 있음.
- **정보 활용 제한**: 유저의 나이, 영화 장르 같은 부가 정보를 활용하기 어려움.

## 💻 코드 예시 (PyTorch)
```python
class MF(nn.Module):
    def __init__(self, num_users, num_items, embed_dim=32):
        super().__init__()
        self.user_embedding = nn.Embedding(num_users, embed_dim)
        self.item_embedding = nn.Embedding(num_items, embed_dim)

    def forward(self, users, items):
        u = self.user_embedding(users)
        i = self.item_embedding(items)
        return (u * i).sum(1)
```
